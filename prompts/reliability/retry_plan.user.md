dispatch_success={{dispatch_ok}}
dispatch_message={{message}}

type={{action_type}}
intent={{intent}}
value={{value}}

failure_reason（页面校验未通过的主要原因）:
{{failure_reason}}

当前 URL：{{page_url}}

页面元素摘要（节选）：
{{dom}}

请输出 JSON。
